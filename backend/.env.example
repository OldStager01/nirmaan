# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
# Set to 'true' to use PostgreSQL, leave unset or 'false' for SQLite (easier for testing)
USE_POSTGRES=false

# PostgreSQL Settings (only needed if USE_POSTGRES=true)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=nirmaan_db
DB_USER=postgres
DB_PASSWORD=your_password

# SQLite Settings (used when USE_POSTGRES=false or unset)
SQLITE_PATH=./database.sqlite

# JWT Secret
JWT_SECRET=nirmaan_techphantom_sih2025_secret_key_change_in_production
JWT_EXPIRE=7d

# CORS
CORS_ORIGIN=http://localhost:3000

# Admin Credentials (Hardcoded for demo)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=Admin@123
ADMIN_EMAIL=admin@nirmaan.gov.in

# User Credentials (Hardcoded for demo)
USER_USERNAME=farmer
USER_PASSWORD=Farmer@123
USER_EMAIL=farmer@nirmaan.gov.in
